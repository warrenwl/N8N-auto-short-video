**先做“半自动 Agent 工作流”，不要一开始追求全自动无人值守**。最稳的路线是：AI 自动生成视频 → 自动剪辑/字幕/封面 → 人工审核一下 → 自动或半自动发布 → 自动回收数据。

## 我建议的整体架构

```
选题库 / 热点库
   ↓
脚本 Agent：标题、脚本、分镜、口播、标签
   ↓
素材生成：图片 / 视频片段 / 配音 / BGM / 字幕
   ↓
自动剪辑：FFmpeg / MoviePy / Remotion / CapCut 脚本化
   ↓
质检 Agent：时长、比例、字幕、敏感词、版权、水印、AI 标识
   ↓
人工确认：通过 / 修改 / 驳回
   ↓
发布 Agent：YouTube / TikTok / 抖音 / 快手 / B站 / Instagram
   ↓
数据回收：播放量、完播率、点赞、评论、涨粉
   ↓
复盘 Agent：下次选题和标题优化
```

## 技术栈建议

如果你想快点落地，我建议：

| 层级       | 推荐工具                                                     |
| ---------- | ------------------------------------------------------------ |
| 工作流编排 | **n8n**（本地已安装）                                        |
| LLM 节点   | GLM-5.1 API（密钥放在 n8n Credentials 或 `.env`，不要写入文档） |
| 视频生成   | FFmpeg/remotion/即梦 API（Access Key 放在 n8n Credentials 或本地私密文件） |
| 图片生成   | 本地ComfyUI                                                  |
| 配音       | 本地VoxCPM                                                   |
| 字幕       | Remotion/Whisper                                             |
| 自动剪辑   | FFmpeg/Remotion                                              |
| 数据库     | PostgreSQL                                                   |
| 文件存储   | 本地文件夹                                                   |
| 审核后台   | n8n Form                                                     |
| 发布       | 各平台官方 API，不能 API 的平台走人工确认                    |

n8n 适合做这种“AI + API + 审核 + 定时任务”的中控台，它官方也主打 AI workflows/agents，并支持自托管；但如果自托管，要做 SSL、2FA、SSO、安全审计、限制危险节点等安全配置。n8n 官方文档也明确建议自托管实例做好 SSL、2FA、安全审计、SSRF 防护、限制节点等措施。

ComfyUI 适合做你的生成引擎，官方文档也说明它的 workflow 本质是节点图，可以通过 API 格式的 JSON 工作流接入自动化系统。

安全约定：API Key、Access Key、平台 OAuth token、n8n `N8N_ENCRYPTION_KEY` 只允许放在 `.env`、n8n Credentials 或密钥管理服务中；不要写进 Markdown、workflow JSON、SQL、Git 记录或聊天记录。已经写入过文档的密钥应当视为已泄露并轮换。

## 平台发布能力建议

| 平台            | 自动发布建议              | 说明                                                         |
| --------------- | ------------------------- | ------------------------------------------------------------ |
| YouTube         | 强烈推荐 API              | YouTube Data API 的 `videos.insert` 支持上传视频和设置元数据；但未验证 API 项目上传的视频可能被限制为私密，需要审核项目。 |
| TikTok          | 可做，但要申请权限        | TikTok Content Posting API 支持 Direct Post，需要注册 app、申请 `video.publish` 权限；未审核客户端发布内容会限制为私密；AI 生成内容还有 `is_aigc` 标记字段。 |
| Instagram Reels | 可做，但偏企业/创作者账号 | Meta 的 Instagram Content Publishing API 支持发布图片、视频、Reels、轮播等内容。 |
| 抖音            | 可做，但要走开放平台      | 抖音开放平台有“内容发布接入方案”，支持第三方系统通过 open_api 直接发布视频或图片；官方也提醒视频审核逻辑与端内一致，水印/logo 可能影响审核和推荐。 |
| 快手            | 可做                      | 快手开放平台文档里有“创建视频/发布视频”，支持小文件直接上传和大文件分片上传。 |
| B站             | 可做                      | B站开放平台有视频管理能力，包括视频稿件发布、删除、查询；视频稿件提交后存在审核过程。 |
| 小红书          | 建议先半自动              | 小红书公开开放能力相对更受限，我建议先做“生成内容 + 生成封面 + 生成标题标签 + 手机端确认发布”，不要依赖模拟登录或 cookie 自动化。 |

## 我建议你先做的 MVP

第一版不要复杂，做这 7 个节点就够了：

1. **选题表**：Notion / Airtable / Google Sheet
    字段：选题、平台、视频类型、状态、发布时间、标题、标签、素材链接。
2. **脚本生成 Agent**
    输入选题，输出：短视频标题、3 秒钩子、正文口播、分镜、字幕、封面文案、标签。
3. **素材生成节点**
    图片用 ComfyUI；视频可以先用 FFmpeg/即梦这类 API 或半自动工具。Runway 现在有面向开发者的 API 文档，可用于把视频生成能力接入自己的产品或工作流。
4. **配音 + 字幕节点**
   本地VoxCPM TTS 生成音频，Remotion/Whisper 反向生成字幕，保证字幕和语音对齐。
5. **自动剪辑节点**
    用 Remotion 拼接：
    `片头钩子 → 主体画面 → 字幕 → BGM → 结尾引导 → 封面图`
6. **人工审核节点**
    这是核心。自动生成的视频很容易出现错字、违禁词、版权素材、画面畸形、平台敏感内容。建议所有视频都先进入“待审核”。
7. **发布节点**
    YouTube / TikTok / 抖音 / B站 / 快手走官方 API。小红书先生成发布包，让你手动确认。

## Agent 不要只做“生成”，还要做“质检”

我建议你做 4 个 Agent：

**1. 选题 Agent**
 负责从热点、竞品、历史数据里找选题。

**2. 创作 Agent**
 负责脚本、分镜、标题、标签、封面文案。

**3. 质检 Agent**
 检查敏感词、版权、AI 标识、视频比例、字幕错别字、是否有平台禁用词、是否疑似搬运。

**4. 运营复盘 Agent**
 每天回收数据，判断哪些标题、封面、时长、开头钩子更有效。

## 视频规格建议

建议先统一做两种模板：

| 类型       | 比例            | 适合平台                          |
| ---------- | --------------- | --------------------------------- |
| 竖屏短视频 | 9:16，1080×1920 | 抖音、快手、TikTok、Reels、小红书 |
| 横屏中视频 | 16:9，1920×1080 | YouTube、B站、西瓜/头条           |

你的 Mac M3 24G 可以做控制台、剪辑、字幕、小规模 ComfyUI 测试；但如果要批量生成视频，建议把视频模型放到云 GPU，Mac 本地只负责调度和审核。

## 一个比较稳的落地方案

我会这样搭：

```
n8n
├── 定时触发：每天读取选题表
├── LLM 节点：生成脚本、标题、标签
├── ComfyUI API：生成封面和关键帧
├── 视频生成 API：生成 3-5 个片段
├── TTS：生成口播
├── Whisper：生成字幕
├── FFmpeg Worker：合成成片
├── 质检 Agent：检查违规、错字、比例、音量
├── 审核表单：你点“通过”
├── 发布 API：按平台发布
└── 数据回收：写回数据库
```
